# コンポジットビデオを3番系統(D-SUB緑 + SOGIN_3)で試す

新しいセッションで着手するための設計と手順。**この文書だけ読めば始められる**ように、
根拠(データシートの該当箇所・コードの行)を全部書いてある。

## 0. 結論の要約

- **TVP7002 はコンポジットをデコードできない。** Digitizer であって Decoder ではなく、
  色副搬送波PLL・直交復調・Y/C分離が丸ごと無い
- ただし **8fsc 線同期サンプラーとしては使える。** 復調は Viewer 側で書く
- **Step 1 はハードもゲートウェアも変更ゼロ。** D-SUB の緑ピンにコンポジットを入れて
  CONFIG を3つ叩くだけで、同期ロックとPLLロックまで確認できる
- 3番系統を使うのは、**緑→SOGタップ(`c_sog3`)があって同じ信号から digitize と
  同期抽出の両方が取れる**のが3番だけだから。SOGIN_2 は CSYNC 専用ピンから
  1/2分圧を経由しているので使えない

## 1. 裏取り済みの事実

| 事実 | 典拠 |
|---|---|
| 型名は "TRIPLE 8-/10-BIT 165-/110-MSPS **VIDEO AND GRAPHICS DIGITIZER**" | データシート表紙 |
| 対応は component (480i〜1080p) と PC graphics (UXGAまで)。**NTSC/PAL/SECAM/CVBS/subcarrier/comb filter の記述はデータシート全文(8821行)に一度も無い** | 全文検索 |
| "composite" が出るのは全部 **composite sync**(複合同期)。"chroma" は既に分離済みCbCrの4:4:4→4:2:2間引きの話だけ | 129, 3264, 3231行 |
| H-PLL の分周は **12bit**(レジスタ01h/02h)、ピクセルクロック **12〜165MHz** | 2120行付近 |
| レジスタ10h bit1 = Green Clamp Select。0=ボトム(既定) / 1=**ミッドレベル** | 10h の記述 |
| レジスタ1Ah bit[7:6] = SOG LPF。00=**2.5MHz** / 01=10MHz / 10=33MHz / 11=バイパス | 1Ah の記述 |
| レジスタ03h = VCO[7:6] + チャージポンプ電流[5:3]。**ICP = 40 × KVCO / (pixels per line)**、VCO=00(Ultra low)は PCLK<36MHz で KVCO=75 | 03h の記述 |
| レジスタ05h=Clamp Start(HSYNCから何クロック後)、06h=Clamp Width | 05h/06h |
| レジスタ08h/09h/0Ah = Blue/Green/Red **Fine Gain** | 08h〜0Ah |
| チャンネルごとに**独立したクランプ・ゲイン・オフセット・ADC** | 機能表 |
| **キャプチャはラインの先頭から `width=2048` で取る**(`hs_offset=0`)ので、**カラーバーストは既に payload に入っている** | `retrocastx_stream.py:879, 1256` |
| TVP→FPGA は **8bit**(`vd_r2`〜`vd_r9`。LSB2bitは未接続) | `main.ato:1704-1711` |
| **伝送は RGB555 固定。ここが唯一の障害** | `retrocastx_capture.py:288`, `retrocastx_stream.py:157` |
| プロトコルには `pixfmt 0=RGB888 / 1=RGB555 / 2=RGB565` の枠がある(555以外は未実装) | `protocol-v0.md:31` |

### CONFIGキーの対応(実装済み)

| キー | 信号 | TVPレジスタ | 現在値 |
|---|---|---|---|
| 0x11 | `cfg_hs_offset` | — (キャプチャ窓) | 0 |
| 0x12 | `cfg_pll_divide` | 01h/02h | 1104 |
| 0x17 | `cfg_video_bw` | 3Fh | 0x0F |
| 0x18 | `cfg_fine_clamp` | 2Ah | 0x87 |
| **0x19** | `cfg_pll_ctl` | **03h** (VCO+チャージポンプ) | 0x18 |
| **0x1A** | `cfg_clamp_start` | **05h** | 0x32 |
| **0x1B** | `cfg_clamp_width` | **06h** | 0x20 |
| 0x22 | `cfg_sync_ctl` | 0Eh | 0x52 |
| 0x50 | `cfg_sog_thresh` | 10h の **[7:3]だけ** | 0x0B |
| 0x5D | `cfg_in_mux2` | 1Ah | 0x12 |

★**レジスタ19h(入力MUX)と10hのクランプ選択ビットにはキーが無い。**
19h はビルド時定数 `MUX1`(=0xAA、全チャネル_3)。10h は下位3bitが
`Cat(C(0x58 & 0x07, 3), cfg_sog_thresh)` で **0固定**(R/G/B全てボトムレベル)。

## 2. サンプリングは 8fsc(1820サンプル/ライン)

4fsc ではなく 8fsc にする。理由は**既存のアンチエイリアスがそのまま最適値になる**こと。

```
                サンプル/ライン  サンプルレート  ナイキスト   既存フィルタ(fc 16.8MHz)の減衰
NTSC 4fsc            910          14.318MHz     7.16MHz     -0.45dB  ← 折り返しに無防備
NTSC 8fsc           1820          28.636MHz    14.32MHz     -2.4dB   ← ちょうど良い
PAL  8fsc           2270          35.469MHz    17.73MHz     -3.5dB   ← ★バッファ2048に入らない
```

既存フィルタは 220Ω + 33pF + TVP入力容量10pF = 43pF → fc = 16.8MHz。

```
3.58MHz (副搬送波)   -0.19dB   ← クロマがフラットに通る
4.2MHz  (輝度上限)   -0.26dB
14.32MHz(ナイキスト) -2.4dB
```

- 1820 は 12bit分周(最大4095)に収まり、28.636MHz は 12〜165MHz の範囲内
- 1820 ≤ `width=2048` なのでキャプチャバッファに全ライン入る
- **PAL は 2270 > 2048 でバッファを超える。** PAL をやるなら 4fsc(1135)に落とすか
  バッファを広げる。国内の実機は 15.7kHz 系が主なので NTSC 優先で進める

## 3. 手順

### Step 1 — 変更ゼロ。同期とPLLロックの確認

**ハードもゲートウェアも Viewer も触らない。** CONFIG を3つ書くだけ。

**ケーブル**: コンポジットの芯線を D-SUB15 の **pin2(緑)**、シールドを **pin7(緑GND)** へ。
HSYNC/VSYNC は繋がない(SOG経路を使う)。75Ω終端は基板側の `ch_g.r_term` にある。

**CONFIG**

| キー | 値 | 意味 |
|---|---|---|
| 0x12 | **1820** | PLL分周 = 8fsc NTSC |
| 0x19 | **0x10** | VCO=00(Ultra low) + チャージポンプ電流=2 |
| 0x1A | **230** (0xE6) | クランプ開始をバーストの後ろへ |
| 0x1B | **30** (0x1E) | クランプ幅 |

チャージポンプの根拠。`ICP = 40 × KVCO / (pixels per line)`、VCO=00 なら KVCO=75:

```
pll_divide 1104 (現状)  40×75/1104 = 2.72  → 3   現在の 0x18 = VCO00/ICP3 と一致
pll_divide 1820 (8fsc)  40×75/1820 = 1.65  → 2   → 0b00_010_000 = 0x10
pll_divide 2270 (PAL)   40×75/2270 = 1.32  → 1   → 0x08
```

クランプ位置の根拠。NTSC の同期立ち下がりを起点にすると:

```
0〜4.7µs      同期チップ
4.7〜5.3µs    ブリーズウェイ
5.3〜7.8µs    カラーバースト (3.58MHz × 9周期)
7.8〜9.4µs    バースト後のバックポーチ  ← ここでクランプする
9.4µs〜       アクティブ映像
```

28.636MHz は 34.93ns/クロックなので 7.8µs=223、9.4µs=269クロック。
**start=230 / width=30 なら 230〜260(8.03〜9.08µs)** で窓の内側に入る。
**バーストの上でクランプすると基準がずれる**ので、既定の 0x32(=50クロック=1.75µs、
同期チップの上)のままでは駄目。

**SOG LPF は変更不要。** `cfg_in_mux2` の既定 0x12 は bit[7:6]=00 = **2.5MHz** で、
3.58MHz のバーストを既に落としている(15kHz機向けにそう決めてあった)。

**入力MUXも変更不要。** `MUX1`=0xAA で SOG/R/G/B すべて _3。

**確認すること**

| CONFIGキー | 期待値 |
|---|---|
| 0x54 syncdet | SOGD=1 / AHS=1 / AVS=1(MSXのC-SYNCで実証済みの 0x6E 相当) |
| 0x55 lines/frame | 262 か 263(インタレース源なら525前後) |
| 0x56 clocks/line | **1820 前後** |

**見え方の期待値**

- **緑一色の絵になる。** コンポジットは緑チャンネルにしか入っていないので、R/B は
  終端されたまま≒0
- 色のある部分に**副搬送波由来のチェッカー/ドットクロールが出る**。これが「復調前の
  クロマがちゃんとサンプルされている」証拠になる
- **バーストは payload に入っているが、この段階では下半分が潰れている**
  (ボトムレベルクランプなのでブランキングがコード0に張り付く)。Step 2 で直す
- 横 1820 相当の細長い絵になる。`offset_px` はラインの絶対位置なので Viewer 側は
  そのまま扱えるはず

**ここで止まったら**: PLLがロックしないときは 0x19 のチャージポンプを 1/2/3 で振る。
同期が取れないときは 0x50(SOG閾値、既定0x0B=124mV)と 0x5D(SOG LPF を 01=10MHz へ)を振る。

### Step 2 — ミッドレベルクランプ(ゲートウェアの小改造)

バーストを丸ごと見えるようにする。**副搬送波の位相ロックに必須。**

```
コンポジット全振幅 = 140 IRE (同期-40 〜 白+100)、カラーバースト = 40 IRE p-p

ボトムレベルクランプ  ブランキングがコード0 → バーストの下半分が切り捨てられる
ミッドレベルクランプ  ブランキングが512(10bit)/128(8bit) → バーストが丸ごと入る
```

`retrocastx_i2c.py:465` が下位3bitを0で固定しているので、**CONFIGキーを1本足す**。

```python
# 現状
self.comb += If(step == WR_REG.index(0x10),
                wval_b.eq(Cat(C(0x58 & 0x07, 3), self.cfg_sog_thresh)))
# 変更案: 3bit の cfg_clamp_sel を足して差し替える(空きキー 0x13 など)
self.cfg_clamp_sel = Signal(3, reset=0x00)     # [2]=Blue [1]=Green [0]=Red
self.comb += If(step == WR_REG.index(0x10),
                wval_b.eq(Cat(self.cfg_clamp_sel, self.cfg_sog_thresh)))
```

コンポジット時は `cfg_clamp_sel = 0b010`(Green だけミッドレベル)。

★**これはモード固有の設定。RGB入力では全チャネルをボトムレベルに戻すこと。**
`retrocastx_i2c.py:233` に「0x58 = R/G/B全てbottom-levelクランプ。崩すと背景が紫がかる」
と実測の記録がある(TVP既定の0x5DはR/Bがミッドレベルで、RGB入力には合っていない)。

空きCONFIGキー: 0x13〜0x16 / 0x2D〜0x2F / 0x36〜0x4F / 0x5E〜0x5F / 0x65 / 0x67以降。

### Step 3 — 8bit で送る

**これが本命の障害。** 現状は `rgb888_to_555()` で 3bit 捨てているので、
コンポジットの復調には精度が足りない。

```
5bit (32コード)  → 4.4 IRE/コード  → バースト  9コード p-p   ← 位相推定が粗すぎる
8bit (256コード) → 0.55 IRE/コード → バースト 73コード p-p   ← 実用十分
```

バースト9コードでは位相推定が粗く、さらにコムフィルタは隣接ラインを引き算するので
ノイズが倍になる。実効クロマ3bit相当でバンディングと色相ずれが出る。
往年の CVBS デコーダICは 4fsc/8bit で商品になっていたので、8bit あれば足りる。

**実装**: `pixfmt` に新しい値を足し、`pix555` の代わりに 8bit を詰める。
`wr.dat_w.eq(Cat(pair_lo, pix555))` は 32bit語に16bitピクセルを2つ入れる形なので、

- **コンポジット単独なら 1バイト/サンプル**の新しい詰め方が要る(帯域 229 Mbps)
- **S-Video(将来)なら `Y8+C8`=16bit で既存の2バイト枠にそのまま乗る**(帯域 466 Mbps)。
  パッキングを触らずに済むのでむしろ簡単

まず動かすだけなら **16bit枠の上位8bitを捨てて下位にYを入れる**のが一番手数が少ない
(帯域は倍食うが 466 Mbps は GbE の半分弱で収まる)。

★**黒トリム最適化は効かない。** 現状は「ラインの中身のある範囲だけ送る」が、
コンポジットは**バーストも含めてライン全体が中身**なので全ライン送ることになる。

### Step 4 — Viewer 側にデコーダを書く

**FPGAではなく Viewer 側でやる。** 理由:

1. **帯域は生を送る方が少ない。** コンポジットは1チャンネル、デコード後は3チャンネル
   ```
   生 1ch 8bit @28.6MSa/s                   = 229 Mbps
   FPGAでデコードしてRGB555 (720×480@60)     = 332 Mbps
   ```
2. **画質の天井が高い。** 良いコムフィルタは動き適応の2D/3D(ライン間だけでなく
   フレーム間も比較)で、フレームバッファを何枚も要る。ソフトなら自然、
   ECP5-25 で外部メモリ帯域を割くのは厳しい
3. **開発の回転が速い。** キャプチャを一度録ればオフラインで何通りも試せる。
   FPGAはビットストリーム再生成とタイミングクロージャが毎回付く
   (`eth_rx` が 123.59MHz で FAIL した経緯がある。`--timing-allow-fail` で
   通ってしまうので**post-route のレポートを見ること**)
4. **プロジェクトの思想に合う。** FPGAは実時間を測って生を送り、Viewerが再構成する

**やること**

- ライン毎に**バーストへ位相ロック**する(ライン番号のパリティから予測するより堅牢。
  RetroCastX が「ドット数・ライン数に頼らず実時間で位置を決める」方針なのと整合する)
- 8fsc なら副搬送波1周期あたり8サンプルなので、直交復調は加減算主体で書ける
- Y/C分離はまずライン単位の1Dコム(隣接ラインとの加減算)。NTSC は 227.5周期/ラインで
  バースト位相が1ラインごとに180°反転するので、加算でY・減算でCが出る
- 余裕が出たら動き適応の2D/3Dコムへ

## 4. 落とし穴

| 落とし穴 | 対処 |
|---|---|
| バーストの上でクランプすると基準がずれる | クランプ開始を 230前後(バースト後)に |
| ボトムレベルクランプではバーストの下半分が消える | Step 2 のミッドレベルクランプ |
| RGB555 のままでは復調精度が足りない | Step 3 の8bit化 |
| PAL 8fsc(2270)はキャプチャバッファ2048に入らない | 4fsc(1135)か バッファ拡張 |
| ミッドレベルクランプをRGB入力に残すと背景が紫がかる | モード固有設定として切り替える |
| 黒トリム最適化が効かず全ライン送ることになる | 帯域見積もりに注意(466 Mbps) |
| SOG閾値を上げ過ぎるとバーストで誤トリガ | 既定の2.5MHz SOG LPF を維持 |

## 5. 将来: S-Video 化(基板発注前に部品だけ置いておく)

コンポジットの一番難しい部分(Y/C分離)は、**S-Video なら丸ごと消える**。
1番系統(`rin_1`/`gin_1`/`sogin_1`、現在すべて未使用終端)が空いているので、
PCB を起こす前に R/C だけ置いておけば後から対応できる。

**既存の3つのシュントコンデンサが既に正しい位置にある**(TVPピン直近)。
値を 33pF に変えればアンチエイリアスとしてそのまま使える。

```
nc_caps[0]      rin_1   → GND   10nF → 33pF   C(色差)チャンネルのAA
nc_caps[2]      gin_1   → GND   10nF → 33pF   Y のAA
nc_sog_caps[0]  sogin_1 → GND    1nF → 33pF   SOG分岐のAA
```

**追加は 0402 が9個 + 1×4ヘッダ + ESD 1個**。定数は既存RGBチャンネルと同一。

```
1×4ヘッダ ──┬── Y  ─┬─ 75Ω ─ GND
             │       ├─ 100nF ─ 220Ω ─ GIN_1    (+既存33pF)
             │       └─ 100nF ─ 220Ω ─ SOGIN_1  (+既存33pF)
             ├── GND
             ├── C  ─┬─ 75Ω ─ GND
             │       └─ 100nF ─ 220Ω ─ RIN_1    (+既存33pF)
             └── GND
```

- ヘッダにしておけば**コネクタを今決めなくて済む**。後からピグテールでRCAでも
  mini-DIN でも付けられる
- **DNPにせず最初から実装してよい。** 75Ωが入力ノードをDC的にGNDへ落とすので、
  信号が来ていなくても未使用入力として静か(データシートが10nFを求める理由は
  「浮かせない」ことなので、これで満たせる)
- **S-Video なら C チャンネルが8bitを丸ごと使える。** チャンネルごとに独立した
  ゲインとクランプを持つので、クロマ振幅(±60 IRE程度)に合わせてゲインを上げられる。
  コンポジット(140 IRE を輝度と分け合う)に比べてクロマ分解能が約4倍
- mini-DIN 4 は Y ピンにコンポジットを流す変換ケーブルが安く手に入るので、
  コネクタ1個で S-Video とコンポジットの両方をカバーできる
