# コントリビューションについて

RetroCastX への貢献を歓迎します。バグ報告・実機での動作報告・改善提案・パッチ、
どれも助かります。特に**手元の実機(X68000 / PC-98 / その他レトロ機)での動作報告**は、
開発者が持っていない機種のタイミングを知る唯一の手段なので、とてもありがたいです。

## まず Issue から

大きめの変更は、実装前に Issue で相談してください。設計の前提(「変換器は馬鹿に徹し、
知性は PC 側に置く」)から外れる提案は、良いアイデアでも取り込めないことがあります。
先に方針をすり合わせたほうが、お互いの時間を無駄にしません。

タイポ修正や明らかなバグの修正は、いきなり PR で構いません。

## ライセンスと DCO

このリポジトリはディレクトリごとにライセンスが異なります。

| ディレクトリ | ライセンス |
|---|---|
| `gateware/` `client/` `host/` | Apache License 2.0 |
| `hardware/` | CERN-OHL-S-2.0 |
| `docs/` | CC BY-SA 4.0 |

**提供いただいた貢献は、そのファイルが属するディレクトリのライセンスの下で
公開されます。** 著作権はあなたに残ります。こちらへの権利譲渡は求めません。

そのうえで、すべてのコミットに **Signed-off-by** 行を付けてください。

```sh
git commit -s -m "コミットメッセージ"
```

これで以下の行が自動的に追加されます。

```
Signed-off-by: 山田太郎 <taro@example.com>
```

この署名は [Developer Certificate of Origin 1.1](DCO)(このリポジトリの `DCO`
ファイル)への同意を意味します。要約すると「このコードは自分が書いたものか、
適切なライセンスの下で持ち込む権利があるものです」という本人の宣言です。
権利の移転は一切起きません。

付け忘れた場合は、直前のコミットなら次で直せます。

```sh
git commit --amend -s --no-edit
git push --force-with-lease
```

複数コミットにまとめて付ける場合:

```sh
git rebase --signoff origin/main
```

PR には CI で署名チェックが走ります。

## `hardware/` への変更について

`hardware/` は CERN-OHL-S-2.0(強い相互性のオープンハードウェアライセンス)です。
このディレクトリへのパッチは、将来の量産・頒布の枠組みに影響する可能性があるため、
**マージ前に個別に相談させてください**。取り込まないという意味ではなく、権利関係を
確認したうえで進めたい、という意図です。

回路の不備の指摘や部品選定の提案は、Issue でいただければこちらで実装します。
そのほうが早いことも多いです。

第三者の設計データ(他プロジェクトの回路図・KiCad ファイル等)をコピーして
持ち込むことはできません。参照した資料がある場合は、出典を PR の説明に書いてください。

## 商標

**"RetroCast X"**(正式名称)および **"RetroCastX"**(ファイル名・識別子等で
用いる短縮形)の名称、ロゴ、アイコンは、上記いずれのライセンスの許諾範囲にも
含まれません。詳細は [NOTICE](NOTICE) を参照してください。

フォークして独自に頒布する場合は、別の名称を使ってください。ライセンス上の
制約ではなく、利用者がサポートの出所を判断できるようにするための商標上の
線引きです。

なお、本設計に基づくハードウェアの製造・頒布を検討されている場合は、
事前にご一報いただけると嬉しいです(**ライセンス上の義務ではありません**)。
既知の問題や実装上の落とし穴を共有できますし、改善を上流に取り込める場合も
あります。

## 開発環境

ビルドと動作確認の手順は [README.md](README.md) を参照してください。
ハードウェアがなくても、送信シミュレータと受信器のループバックで
プロトコル周りの動作は確認できます。

コードのスタイルは周囲に合わせてください。コメントは日本語で構いません
(既存コードもそうなっています)。「なぜそうしたか」が読み取れるコメントを
歓迎します。
