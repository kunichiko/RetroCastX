# Python
__pycache__/
*.pyc
.venv/

# FPGA build artifacts
gateware/build/

# Receiver dumps
*.ppm

# macOS
.DS_Store

# Claude Code local settings
.claude/settings.local.json

# atopile
hardware/*/build/
.ato/
standalone/

# gateware dev env
gateware/.venv/
gateware/litex-src/
gateware/litex_setup.py

# Rust client
client/target/

# フラッシュ退避(工場LED受信カードFW等、コミット不要のバイナリ)
gateware/backups/

# 保管したビットストリーム(A/B比較用。バイナリはコミットしない)
gateware/bitstreams/

# 実機から録音した確認用WAV(生成物)
*.wav

# メーカー配布のデータシート(再配布可否が不明なのでコミットしない)。
# docs/datasheets/README.md に入手元だけを残す
docs/datasheets/*.pdf

# KiCad の編集ロック(そのマシンでの編集中を示す一時ファイル。追跡しない)
*.lck
