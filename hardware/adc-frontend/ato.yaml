requires-atopile: ^0.14.0

paths:
  src: ./
  layout: ./layouts

builds:
  default:
    entry: main.ato:App
    # `ato build` は全ターゲット成功(netlist/PCB/BOM/pinout/power-tree/stackup)。
    # 成果物: layouts/default/default.kicad_pcb, build/builds/default/default.bom.csv
